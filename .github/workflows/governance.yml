name: Governance

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  isolation:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check fleet isolation
        run: python scripts/check_fleet_isolation.py
